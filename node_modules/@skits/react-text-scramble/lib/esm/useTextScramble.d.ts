import { RevealMode } from './obfuscator';
declare type TextScrambleConfig = {
    characters: string;
    exclude: string[];
    speed: number;
};
declare type TextScrambleOptions = {
    [K in keyof TextScrambleConfig]?: TextScrambleConfig[K];
};
export declare const useTextScramble: (text: string, options?: TextScrambleOptions | undefined) => {
    state: import("./reducer").BaffleState;
    reveal: (revealSpeed: any, delay: any, revealMode: RevealMode, onRevealComplete?: (() => void) | undefined) => void;
    start: () => void;
    stop: () => void;
};
export {};
