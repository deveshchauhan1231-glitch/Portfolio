"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.sample = exports.mapString = exports.getTruthyIndices = void 0;
/**
 * Get an array of indices of truthy values from an array.
 *
 * @param array The array to test
 */
function getTruthyIndices(array) {
    return array
        .map(function (item, index) {
        if (!item)
            return false;
        return index;
    })
        .filter(function (item) { return item !== false; });
}
exports.getTruthyIndices = getTruthyIndices;
/**
 * Transform each character in a string.
 *
 * @param string The string to transform
 * @param fn the transform function to use when mapping through the strings characters
 */
function mapString(string, fn) {
    return string.split('').map(fn).join('');
}
exports.mapString = mapString;
/**
 * Get a random item from an array.
 *
 * @param array The array to sample from
 */
function sample(array) {
    return array[Math.floor(Math.random() * array.length)];
}
exports.sample = sample;
