/**
 * Get an array of indices of truthy values from an array.
 *
 * @param array The array to test
 */
export declare function getTruthyIndices<T>(array: T[]): (number | false)[];
/**
 * Transform each character in a string.
 *
 * @param string The string to transform
 * @param fn the transform function to use when mapping through the strings characters
 */
export declare function mapString(string: string, fn: (character: string, index: number) => void): string;
/**
 * Get a random item from an array.
 *
 * @param array The array to sample from
 */
export declare function sample<T>(array: T[]): T;
