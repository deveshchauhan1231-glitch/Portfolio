"use strict";
var __assign = (this && this.__assign) || function () {
    __assign = Object.assign || function(t) {
        for (var s, i = 1, n = arguments.length; i < n; i++) {
            s = arguments[i];
            for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
                t[p] = s[p];
        }
        return t;
    };
    return __assign.apply(this, arguments);
};
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.useTextScramble = void 0;
var react_1 = require("react");
var utils_1 = require("./utils");
var obfuscator_1 = __importDefault(require("./obfuscator"));
var reducer_1 = require("./reducer");
var constants_1 = require("./constants");
var defaultConfig = {
    characters: constants_1.SYMBOLS,
    exclude: [' '],
    speed: constants_1.DEFAULT_SCRAMBLE_SPEED,
};
var useTextScramble = function (text, options) {
    var config = (0, react_1.useMemo)(function () { return (__assign(__assign({}, defaultConfig), options)); }, [options]);
    var obfuscator = (0, react_1.useMemo)(function () { return new obfuscator_1.default(text); }, [text]);
    var timer = (0, react_1.useRef)(null);
    var startInterval = (0, react_1.useRef)(null);
    var revealInterval = (0, react_1.useRef)(null);
    var _a = (0, react_1.useReducer)(reducer_1.baffleReducer, __assign(__assign({}, reducer_1.initialState), { output: obfuscator.render(config.characters.split(''), config.exclude) })), state = _a[0], dispatch = _a[1];
    /**
     *
     */
    var obfuscate = (0, react_1.useCallback)(function () {
        dispatch({
            type: 'SET_OUTPUT',
            payload: obfuscator.render(config.characters.split(''), config.exclude),
        });
    }, [config.characters, config.exclude, obfuscator]);
    /**
     *
     */
    var stop = (0, react_1.useCallback)(function () {
        dispatch({ type: 'STOP_BAFFLE' });
        startInterval.current && clearInterval(startInterval.current);
        revealInterval.current && clearInterval(revealInterval.current);
    }, []);
    /**
     *
     */
    var start = (0, react_1.useCallback)(function () {
        dispatch({ type: 'START_BAFFLE' });
        startInterval.current && clearInterval(startInterval.current);
        startInterval.current = setInterval(obfuscate, config.speed);
    }, [config.speed, obfuscate]);
    /**
     * Start a new interval, obfuscating fewer characters
     * on each cycle at pace to finish within duration
     * milliseconds. Optionally, delay by delay milliseconds.
     *
     * Once all elements are revealed, call stop() and
     * initialize each element.
     */
    var reveal = (0, react_1.useCallback)(function (revealSpeed, delay, revealMode, onRevealComplete) {
        if (revealSpeed === void 0) { revealSpeed = 0; }
        if (delay === void 0) { delay = 0; }
        var interval = revealSpeed < config.speed ? config.speed : revealSpeed;
        var run = function () {
            revealInterval.current && clearInterval(revealInterval.current);
            revealInterval.current = setInterval(function () {
                // If all elements are revealed, stop and init
                if (!(0, utils_1.getTruthyIndices)(obfuscator.bitmap).length) {
                    stop();
                    obfuscator.init();
                    // Run provided callback if available
                    onRevealComplete && onRevealComplete();
                    return;
                }
                obfuscator.decay(revealMode);
            }, interval);
        };
        timer.current = setTimeout(run, delay);
        start();
    }, [config.speed, obfuscator, start, stop]);
    /**
     * Clean up intervals on unmount
     */
    (0, react_1.useEffect)(function () {
        return function () {
            startInterval.current && clearInterval(startInterval.current);
            revealInterval.current && clearInterval(revealInterval.current);
            timer.current && clearTimeout(timer.current);
        };
    }, []);
    return { state: state, reveal: reveal, start: start, stop: stop };
};
exports.useTextScramble = useTextScramble;
